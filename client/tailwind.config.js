const defaultTheme = require('tailwindcss/defaultTheme')

module.exports = {
    purge: [],
    theme: {
        extend: {
            fontFamily: {
                poppins: ['Poppins'],
            },
        },
    },
    variants: {},
    plugins: [],
}